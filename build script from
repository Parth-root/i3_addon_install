apt update && apt upgrade
#cassandra use more rame always stop after using it
#sudo systemctl start cassandra
stow -nvSt ~ window-manager/
#n for not instll just chack
#v verbose
# S for stow to run
# t i dont know
#stow --adopt -nvSt ~ window-manager/     alternate
sudo apt-get install ninja-build gettext cmake unzip curl
 git clone https://github.com/neovim/neovim
 git clone https://github.com/AnupPainuly/dotfiles
  polybar dir than
  ./launch.sh 
  xrandr display error fix 
 nerdfont
 # mv everything to new folder in same folder  mkdir -p jetbrains && mv * $_
# this work mkdir -p jetbrains && mv Jetbrains* $_
# install font fc-cache -fv
  curl -sS https://starship.rs/install.sh | sh
  ./autotiling 
  pip3
  sudo apt install python3-pip
  pip3 install i3ipc
  321  ./autotiling 
  rofi-theme-selector
  nvim picom.conf 
 
